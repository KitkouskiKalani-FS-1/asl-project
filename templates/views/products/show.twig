{% extends "layouts/base.twig" %}

{% block main_content %}
    <h2>About {{product.name}}</h2>
    <ul styles="list-style:none;">
        <li>
            <label>Id: </label> {{product.id}}
        </li>
        <li>
            <label>Name: </label> {{product.name}}
        </li>
    </ul>
    <p>
        <a href="/products/{{product.id}}/edit">Edit</a> | <a method= "POST" href="/products/{{product.id}}/delete">Delete</a>
    </p>
{% endblock %}