{% extends "layouts/base.twig" %}

{% block main_content %}
    <h2>About {{variant.title}}</h2>
    <ul>
        <li>
            <label>Id: </label> {{variant.id}}
        </li>
        <li>
            <label>Title: </label> {{variant.title}}
        </li>
        <li>
			<label>Slug: </label> {{ variant.slug }}
		</li>
		<li>
			<label>Price: </label> {{ variant.price }}
		</li>
		<li>
			<label>Inventory: </label> {{ variant.inventory }}
		</li>
        <li>
			<label>Variant Description: </label> {{ variant.description }}
		</li>
        <li>
            <label>Product: </label> {{product.title}}
        </li>
    </ul>
    <p>
        <a class="underline underline-offset-1" href="/variants/{{variant.id}}/edit">Edit</a> | <a class="underline underline-offset-1" method= "POST" href="/variants/{{variant.id}}/delete">Delete</a>
    </p>
{% endblock %}